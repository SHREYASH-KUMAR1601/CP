#include <bits/stdc++.h>
using namespace std;
const int N = 0;

#define IOS ios_base::sync_with_stdio(0);cin.tie(0);cout.tie(0);

int main() {
	IOS;

	int n;
	cin >> n;

	while (n--) {
		int x, y;
		cin >> x >> y;

		if (y >= -1) {
			cout << "YES" << endl;

		} else {
			cout << "NO" << endl;
		}
	}


}